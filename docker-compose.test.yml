services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: guniversity_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5433:5432'
    tmpfs:
      - /var/lib/postgresql/data
